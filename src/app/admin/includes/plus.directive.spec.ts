import { PlusDirective } from './plus.directive';

describe('PlusDirective', () => {
  it('should create an instance', () => {
    const directive = new PlusDirective();
    expect(directive).toBeTruthy();
  });
});
